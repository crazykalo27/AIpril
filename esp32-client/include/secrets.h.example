/**
 * secrets.h.example — Copy to include/secrets.h if needed.
 *
 * Cable-only mode: No secrets required. ESP32 talks to server over USB.
 * API keys (OpenAI, Google) live on the server.
 */

#ifndef SECRETS_H
#define SECRETS_H

// No secrets needed for cable mode

#endif
